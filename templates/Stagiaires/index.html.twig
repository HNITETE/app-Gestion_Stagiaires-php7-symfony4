{% extends 'base.html.twig' %}
{% block ab %}
    <section class="content-header">
    <h1>
        Gérer
        <small>Stagiaires</small>
    </h1>
</section>
{% endblock %}
{% block body %}
    <table class="table" >
        <thead class="thead-dark text-center">
        <tr>
            <th scope="col">Code</th>
            <th scope="col">nom</th>
            <th scope="col">prenom</th>
            <th scope="col">sexe </th>
            <th scope="col">email</th>
            <th scope="col">Details</th>
            <th scope="col">Modifier</th>
            <th scope="col">Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for stg in stgs %}
            <tr>
                <th>{{ stg.codest }}</th>
                <td scope="row">{{ stg.nomst }}</td>
                <td>{{ stg.prenomst }}</td>
                <td>{{ stg.sexest }}</td>
                <td>{{ stg.email }}</td>
                <td><a href="{{ path('detail_stg', {'id':stg.codest})  }}"><img src="dist/img/detail.png " width="30px" height="30px"></a></td>
                <td><a href="{{ path('edit_stg', {'id':stg.codest})  }}"><img src="dist/img/icon.png " width="30px" height="30px"></a></td>
                <td><a href="{{ path('delete_stg', {'id':stg.codest})  }}"><img src="dist/img/delete.png " width="30px" height="25px"></a></td>

            </tr>

        {% endfor %}


        </tbody>
    </table>



{% endblock %}