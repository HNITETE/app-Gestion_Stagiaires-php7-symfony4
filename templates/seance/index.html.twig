{% extends 'base.html.twig' %}
{% block ab %}
    <section class="content-header">
        <h1>
            Gérer
            <small>Seances</small>
        </h1>
    </section>
{% endblock %}
{% block body %}
    <table class="table" >
        <thead class="thead-dark">
        <tr>
            <th scope="col">Code S</th>
            <th scope="col">Date S</th>
            <th scope="col">heureDB </th>
            <th scope="col">heureFN</th>
            <th scope="col">remume</th>
            <th scope="col">Details</th>
            <th scope="col">Modifier</th>
            <th scope="col">Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for snc in sncs %}
            <tr>
                <th scope="row">{{ snc.codese }}</th>
                <td>{{ snc.datese | date('F j, Y') }}</td>
                <td>{{ snc.heuredbse | date('h , m') }}</td>
                <td>{{ snc.heurefnse | date('h , m')}}</td>
                <td>{{ snc.resmmese }}</td>
                <td><a href="{{ path('detail_snc', {'id':snc.codese})  }}"><img src="dist/img/detail.png " width="30px" height="30px"></a></td>
                <td><a href="#"><img src="dist/img/icon.png " width="30px" height="30px"></a></td>
                <td><a href="{{ path('delete_cns', {'id':snc.codese})  }}"><img src="dist/img/delete.png " width="30px" height="25px"></a></td>

            </tr>

        {% endfor %}


        </tbody>
    </table>



{% endblock %}